// depcrecate. we're not using ct lists
import axios from "axios";

export const getCTLists = async () => {
  //const { data } = await axios.get('/api/ctlist');
  const data = undefined;
  return data;
};
