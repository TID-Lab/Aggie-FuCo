import CredentialsSection from "./CredentialsSection";

// Standalone Credentials page. The list/create logic lives in CredentialsSection
// so it can be reused inside the consolidated Connections page.
const CredentialsIndex = () => <CredentialsSection />;

export default CredentialsIndex;
